% ----------------------------------------------------------------------
\begin{frame}[fragile,c]{Numbers}

\vspace{-0.5cm}

\large

%
% code
%
\begin{semiverbatim}
\alert<2>{
next(1,2). next(2,3). next(3,4). next(4,5).
odd(1).
}\alert<3-8>{
even(N) :-  odd(M), next(M,N).
 odd(N) :- even(M), next(M,N).
}
\end{semiverbatim}

\vspace{-0.75cm}

%
% figure
%
\visible<2->{

\begin{center}

\begin{tikzpicture}[examples]

% smodels
\node[align=center, on grid] (smodel) at (0,0){
\begin{tabular}{r@{\ }l}
\alert<2,3>{\texttt{next(1,2)}} & \alert<2,4>{\texttt{next(2,3)}}\\
\alert<2,5>{\texttt{next(3,4)}} & \alert<2,6>{\texttt{next(4,5)}}\\
\visible<2->{
\alert<2-3>{\texttt{odd(1)}} \visible<3->{& \alert<3-4,8>{\texttt{even(2)}}\\
\visible<4->{
\alert<4-5,8>{\texttt{odd(3)}} \visible<5->{& \alert<5-6,8>{\texttt{even(4)}}
}}}}
\end{tabular}
\\
\begin{tabular}{c}
    \visible<6->{\alert<6,8>{\texttt{odd(5)}}}
\end{tabular}


% \visible<6->{\alert<6,8>{\texttt{odd(5)}}}
};

% ellipses
\visible<2>  {\draw         (-1mm,6mm)   ellipse [thick, x radius=3cm, y radius = 1cm   ];}
\visible<3>  {\draw[dashed] (-1mm,6mm)  ellipse [thick, x radius=3cm, y radius = 1.15cm];}
\visible<4>  {\draw[dashed] (-1mm,4.5mm)  ellipse [thick, x radius=3cm, y radius = 1.4cm ];}
\visible<5>  {\draw[dashed] (-1mm,3.5mm)     ellipse [thick, x radius=3cm, y radius = 1.6cm];}
\visible<6>  {\draw[dashed] (-1mm,2.5mm)     ellipse [thick, x radius=3cm, y radius = 1.8cm ];}
\visible<7>  {\draw[dashed] (-1mm,2.5mm)     ellipse [thick, x radius=3cm, y radius = 1.8cm ];}
\visible<8-9>{\draw         (-1mm,2.5mm)     ellipse [thick, x radius=3cm, y radius = 1.8cm ];}

% ok
\visible<9>{\node[any] (ok) at ($ (smodel) + (3cm,-1cm) $) {\BigBigOK};}

\end{tikzpicture}

\end{center}
} % \visible

\end{frame}
